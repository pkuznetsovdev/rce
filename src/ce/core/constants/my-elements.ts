import { Text, Image, Block, List, Link, Divider, Button } from "../templates";

export const MY_ELEMENTS_BY_NAME = {
    text: Text,
    image: Image,
    block: Block,
    list: List,
    link: Link,
    divider: Divider,
    button: Button,
} as const;
